# TASK-001：建立可运行、可测试的桌面应用工程骨架

## 任务状态

- 状态：`已完成`
- 优先级：`P0`
- 类型：`工程初始化 / 架构基座`
- 前置任务：无
- 后续直接任务：工作区目录选择与只读文件树
- 项目基线：[PROJECT_BASELINE.md](./PROJECT_BASELINE.md)

## 一、为什么这是第一步

当前仓库只有项目概述文档，尚无源码、依赖、构建脚本、测试设施和进程边界。项目后续的工作区访问、TXT 编辑、DOCX 处理和状态持久化，都依赖一个稳定的 Electron 主进程、受控的 preload 接口以及 React 渲染进程。

本任务只建立最小工程基座并打通开发闭环，不提前实现产品功能。完成后，后续智能体应能在明确边界内增量开发，而不必重新决定目录结构、运行方式和安全模型。

## 二、任务目标

创建一个面向 Windows 10/11 的 Electron + React + TypeScript 桌面应用，使其满足以下最小闭环：

1. 可通过单条命令启动开发环境并显示桌面窗口；
2. 可通过单条命令完成生产构建；
3. React 渲染进程显示“文枢”应用壳层占位界面；
4. 主进程、preload 和渲染进程具有清晰、受控的安全边界；
5. 类型检查、代码检查、格式检查和基础测试可执行；
6. 目录结构足以承载后续模块，但不提前实现复杂抽象。

## 三、固定技术约束

执行智能体必须遵循以下约束：

- 使用 Electron、React、TypeScript 和 Vite；
- 使用 `npm` 管理依赖，并提交锁文件；
- TypeScript 开启严格模式；
- 使用 Vitest 建立测试入口；
- 使用 ESLint 和 Prettier；
- 渲染进程必须设置 `nodeIntegration: false`；
- 必须启用 `contextIsolation: true`；
- 渲染进程不得直接导入或调用 Node.js 文件系统 API；
- 桌面能力只能由 preload 通过 `contextBridge` 暴露窄接口；
- 不引入 SQLite、云服务、账号系统、插件框架或 AI SDK；
- 不为了未来需求建立微服务、依赖注入容器或通用插件系统；
- 依赖版本应选择执行时稳定且彼此兼容的版本，并在任务完成说明中记录关键选择。

## 四、范围内工作

### 4.1 工程与脚本

- 初始化 `package.json` 和 npm 锁文件；
- 配置 Electron 主进程、preload、Vite React 渲染进程的开发与构建流程；
- 提供至少以下 npm scripts（名称可以等价调整，但职责必须完整）：
  - `dev`：启动 Electron 开发环境；
  - `build`：生成生产构建产物；
  - `typecheck`：执行 TypeScript 类型检查；
  - `lint`：执行静态检查；
  - `format:check`：检查格式；
  - `test`：执行一次基础测试；
- 配置 `.gitignore`，排除依赖、构建产物、缓存和本地临时文件；
- 编写简短 `README.md`，说明环境要求、安装、开发、检查和构建命令。

### 4.2 进程结构

至少建立以下职责边界：

- `main`：Electron 生命周期、窗口创建和安全配置；
- `preload`：受控桌面 API 的唯一渲染进程入口；
- `renderer`：React 界面，不拥有直接文件系统权限；
- `shared`：仅在确有跨进程类型需要时存放共享类型，禁止放置依赖运行环境的实现。

preload 在本任务中只需暴露一个无副作用的最小接口，例如获取运行平台或应用版本，用它证明类型化桥接链路可用。不要在本任务中暴露通用 IPC 调用器、任意通道调用能力或文件系统 API。

### 4.3 最小应用壳层

渲染进程显示一个简洁的占位工作台，用于证明 React 和样式加载正常。至少包含：

- 产品名“文枢”；
- 左侧栏占位区域；
- 中央编辑区占位区域，并保持为视觉主体；
- 底部状态栏占位区域；
- 从 preload 接口读取的一项只读环境信息。

本任务不要求完成正式视觉设计。布局只需稳定、可辨认，并为后续替换成真实组件提供清晰结构。

### 4.4 质量基线

- 至少添加一个能够实际运行的 Vitest 测试；
- 测试应覆盖纯逻辑或轻量 React 渲染，不应为了一个占位测试引入复杂 Electron 端到端设施；
- 类型检查应分别覆盖主进程/preload 与渲染进程，避免不同运行环境的全局类型互相污染；
- 构建和检查命令在干净安装后应可重复执行。

## 五、明确不在本任务范围内

- 打开或恢复工作区；
- 目录选择器、文件树和文件监听；
- TXT 或 DOCX 的读取、编辑与保存；
- 多标签页状态；
- Zustand 业务状态；
- CodeMirror、Tiptap、Mammoth、docx、JSZip；
- 搜索、设置持久化和 `electron-store`；
- Windows 安装包或自动更新；
- Playwright/Electron 端到端测试；
- 完整菜单、快捷键、主题系统和正式 UI 设计；
- AI 或 Agent 功能。

如执行中发现上述能力对工程启动并非必需，不得顺手加入。

## 六、建议目录结构

以下结构为建议而非必须逐字照搬；若调整，必须保持职责等价且在完成说明中解释原因。

```text
.
├─ src/
│  ├─ main/
│  │  └─ index.ts
│  ├─ preload/
│  │  └─ index.ts
│  ├─ renderer/
│  │  ├─ components/
│  │  ├─ App.tsx
│  │  ├─ main.tsx
│  │  └─ styles/
│  └─ shared/
│     └─ desktop-api.ts
├─ tests/                 # 也可采用源码旁置测试
├─ index.html
├─ package.json
├─ tsconfig*.json
├─ vite.config.*
├─ eslint.config.*
├─ .prettierrc*
├─ .gitignore
└─ README.md
```

## 七、建议执行顺序

1. 检查本机 Node.js/npm 环境，并记录实际版本；
2. 初始化 npm 工程并安装最小依赖；
3. 配置相互隔离的 TypeScript 编译环境；
4. 建立 Electron 主进程和安全的 BrowserWindow 配置；
5. 建立类型化 preload 桥接和渲染端全局类型声明；
6. 建立 Vite + React 页面和最小壳层；
7. 配置 ESLint、Prettier、Vitest 与 npm scripts；
8. 补充基础测试和 README；
9. 依次运行全部验收命令，修复错误后再提交任务结果。

## 八、验收标准

只有以下条件全部满足，本任务才可标记为完成：

- [ ] `npm install` 在干净环境中成功，并生成/使用锁文件；
- [ ] `npm run dev` 能启动 Electron 窗口；
- [ ] 窗口内能看到 React 渲染的“文枢”最小壳层，而不是空白页；
- [ ] 页面能通过类型化 preload API 显示一项只读环境信息；
- [ ] BrowserWindow 明确使用 `nodeIntegration: false` 和 `contextIsolation: true`；
- [ ] 渲染进程不存在直接 Node.js 文件系统访问；
- [ ] preload 未暴露通用 `ipcRenderer`、任意 IPC 通道或其他宽权限对象；
- [ ] `npm run typecheck` 成功；
- [ ] `npm run lint` 成功；
- [ ] `npm run format:check` 成功；
- [ ] `npm test` 成功，且至少有一个有效测试；
- [ ] `npm run build` 成功；
- [ ] README 中的安装和运行说明与实际命令一致；
- [ ] 未实现第四、五节之外的产品功能。

## 九、交付物

执行智能体应交付：

1. 可运行的工程源码与配置；
2. npm 锁文件；
3. 基础测试；
4. 项目运行说明；
5. 一份简短完成报告，至少写明：
   - 新增/修改的关键文件；
   - 关键依赖及选择原因；
   - 实际执行过的验证命令与结果；
   - 未解决问题或已知限制；
   - 是否满足本文件全部验收标准。

## 十、失败处理与决策规则

- 若脚手架默认配置违反 Electron 安全约束，应修改脚手架配置，不得降低安全标准；
- 若依赖之间不兼容，优先选取稳定兼容组合，不得使用 `--force` 或长期保留依赖冲突；
- 若某项工具配置显著扩大任务范围，应采用满足验收标准的最小配置；
- 若必须偏离项目基线或本任务的固定约束，应停止相关实现，在完成报告中说明问题、替代方案、收益、风险和迁移成本，等待项目所有者决定；
- 不得通过删除有效测试、关闭严格类型检查或忽略 lint 规则来伪造通过结果。

## 十一、完成后的下一任务入口

本任务完成后，下一开发任务应是一个纵向最小切片：通过受控 preload/IPC 打开本地文件夹，安全读取目录结构，并在左侧栏展示只读文件树。该后续任务不属于本次交付。
