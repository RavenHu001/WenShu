# 文枢测试指南

本文档说明 Task 1 至 Task 12 的自动检查、Windows 桌面冒烟与人工验收。Task 11 在不改变 Task 1–10 文件/保存/搜索生命周期的前提下新增统一中文外壳、设计 token 与 SVG、可调/可折叠侧栏、文件树上下文菜单与键盘入口、同工作区内部拖拽、DOCX 连续居中画布、工具栏溢出、搜索结果层级和 toast。自动保存、文件系统监听与会话恢复仍不属于已有能力。Task 7–10 的详细既有清单继续保留在 3.5–3.8；Task 11 的桌面外壳、缩放、无障碍和视觉基线见 2.10 与 3.9。Task 12 的打包门禁、理论兼容性和实机验证边界见 2.11，并以 [TASK-012 完成报告](./TASK_012_COMPLETION_REPORT.md) 为最终证据来源。

## 1. 前置条件

- Windows 10 或 Windows 11；
- Windows PowerShell 5.1 或更高版本；
- 命令在仓库根目录执行。

上述是开发测试环境范围。Task 12 当前 portable/NSIS 只声明 Windows 10 x64 理论兼容；Windows 10
实机验证留到后续开发阶段，Windows 11 矩阵已移到未来可选工作。

首次使用时初始化项目本地 Node.js 和依赖：

```powershell
.\scripts\bootstrap.cmd
.\scripts\npm.cmd ci
```

预期工具版本：

```powershell
.\scripts\node.cmd --version
.\scripts\npm.cmd --version
```

- Node.js：`v22.15.0`；
- npm：`10.9.2`。

下载失败时参见[开发环境说明](./DEVELOPMENT_ENVIRONMENT.md)中的镜像配置和故障处理。

## 2. 自动检查

运行完整质量检查：

```powershell
.\scripts\npm.cmd run check
```

该命令依次验证：

1. Electron 配置、主进程、preload、渲染进程和测试代码的 TypeScript 类型；
2. ESLint 静态检查；
3. Prettier 格式检查；
4. Vitest 单元测试。

验收标准：命令退出码为 `0`，没有 TypeScript、ESLint 或格式错误，全部测试通过。

需要单独定位问题时，可以分别运行：

```powershell
.\scripts\npm.cmd run typecheck
.\scripts\npm.cmd run lint
.\scripts\npm.cmd run format:check
.\scripts\npm.cmd test
```

### 2.1 Task 4 实施前基线门禁

Task 4 将首次引入用户文件写入，因此开始实现前必须确认：

- Windows 检出环境中的换行策略明确，`format:check` 可重复通过；
- Vitest worker、临时目录和符号链接能力探测不会导致整个测试文件超时或跳过；
- 不支持真实 symlink/junction 的环境只条件跳过对应少量集成用例，并由 mock 测试确定性覆盖拒绝分支；
- Task 2、Task 3 的测试实际执行并通过；
- `check` 与 `build` 不并行运行且均通过。

2026-08-04 的一次 Windows 受控环境检查发现 Prettier 行尾差异和 Vitest worker / 初始化超时。它们必须在 Task 4 WP0 中复现、归因并处理，不能通过删除测试、整文件跳过或单纯增加超时掩盖。

### 2.2 Task 5 实施前基线门禁

Task 5 将把单文档 renderer 状态扩展为多标签会话，但不应改变 Task 4 的文件系统权限和安全保存协议。开始实现前必须确认：

- `main` 已包含 Task 4，工作树修改已识别并受到保护；
- 当前 `check` 与 `build` 依次执行并通过；
- TXT 读取、保存、IPC、preload、工作区组件和窗口关闭测试均实际执行；
- 单标签编辑、保存期间继续输入、冲突、混合换行和未保存保护行为可重复；
- Task 5 每个工作包继续运行完整 `check` 和 `build`，不得用新多标签测试替代既有安全回归。

2026-08-06 的规划基线为：9 个测试文件通过，224 个用例通过，2 个真实符号链接用例因本机权限条件跳过，生产构建通过。实现中的实际结果应记录在各工作包报告中，最终以 `TASK_005_COMPLETION_REPORT.md` 为准。

### 2.3 Task 5 完成后的基线

Task 5 全部工作包完成后（见 [TASK-005 完成报告](./TASK_005_COMPLETION_REPORT.md)），实测基线与规划一致：

- 11 个测试文件通过，305 个用例通过，2 个真实符号链接用例因本机权限条件跳过（拒绝分支由 mock 确定性覆盖）；
- 类型检查（5 个 tsconfig）、ESLint（--max-warnings=0）、Prettier 格式检查全部通过；
- 完整 `check` 与 `build` 依次执行并通过；
- Task 4 的读取器 49 项、保存器 51 项、文档 IPC 33 项、preload 13 项、窗口关闭 8 项、工作区扫描 11 项全部原样回归；
- 开发模式与生产构建桌面冒烟均通过（窗口存活、无错误日志）。

### 2.4 Task 6 实施前基线门禁

Task 6 将新增主进程工作区只读搜索、取消协议、结果定位和 CodeMirror 当前文件查找替换。开始实现前必须确认：

- Task 5 完成后的 11 个测试文件、305 个通过用例和 2 个条件跳过用例可重复；
- 完整 `check` 与 `build` 依次执行并通过；
- 当前工作区打开/刷新、多标签身份、受控 TXT 读取、安全保存和未保存保护均无回归；
- 工作区搜索根只能来自主进程会话，搜索请求不得携带根路径或绝对路径；
- 工作区搜索的数据源、固定上限、取消、迟到结果和 revision 过期定位规则已按 [TASK-006 规划](./TASK_006_TXT_SEARCH_FIND_REPLACE.md) 第四节冻结；
- 每个工作包继续运行完整 `check` 和 `build`，不得以新增搜索测试替代 Task 1 至 Task 5 的既有回归。

Task 6 完成前，本指南第 3 节仍只验收 Task 5 的实际能力；完成后由 Task 6 WP7 按任务第 8.7 节把搜索与查找替换清单并入本指南。

### 2.5 Task 6 完成后的基线

Task 6 全部工作包完成后（见 [TASK-006 完成报告](./TASK_006_COMPLETION_REPORT.md)），实测基线与规划一致：

- 19 个测试文件通过，463 个用例通过，3 个真实符号链接用例因本机权限条件跳过（拒绝分支由 mock 确定性覆盖）；
- 类型检查（5 个 tsconfig）、ESLint（--max-warnings=0）、Prettier 格式检查全部通过；
- 完整 `check` 与 `build` 依次执行并通过；
- Task 1 至 Task 5 的既有测试全部原样执行并通过（读取器 49、保存器 51、文档 IPC 33、preload 契约 16、窗口关闭 8、工作区扫描 11、工作区组件 22、标签不变量 23、标签转移 32、多标签组件 64）；
- 搜索契约 14、匹配器 42、搜索器 25、搜索 IPC 26、搜索 controller 14、搜索侧栏 13、结果定位 12、查找替换 9 用例全部通过；
- 开发模式与生产构建桌面冒烟均通过（窗口存活、无错误日志）；
- 接近 1000 TXT 工作区实测：1000 文件搜索约 66 ms、取消响应约 3 ms（2026-08-08 实测，见完成报告性能观察）；
- 第 8.7 节手工界面清单由项目所有者于 2026-08-08 执行并全部通过。

### 2.6 Task 7 实施前基线门禁

Task 7 将首次引入 DOCX 不可信 ZIP/OOXML 解析、结构化富文本模型、Tiptap/ProseMirror 会话、保存前备份和 DOCX 覆盖写入。开始实现前必须确认：

- Task 6 完成后的 19 个测试文件、463 个通过用例和 3 个条件跳过用例可重复，或对当前环境差异形成可核对说明；
- 完整 `check` 与 `build` 依次执行并通过；
- Windows 临时 junction 清理不得因 `EBUSY` 使整个读取测试文件跳过；React 组件测试不得遗留未等待的 `act(...)` 警告；
- TXT 读取、安全保存、多标签、窗口关闭、查找替换、工作区搜索和结果定位均无回归；
- 已按 [TASK-007 规划](./TASK_007_DOCX_BASIC_EDIT_SAFE_SAVE.md) 第 3.3 节准备不含隐私的 DOCX 夹具；
- 已用夹具验证 Mammoth、Tiptap/ProseMirror、`docx` 和 JSZip 的最小闭环，并冻结实际兼容性矩阵、模型预算和依赖；
- 已明确外部 Office 手工验证程序与版本；
- 每个 Task 7 工作包继续运行完整 `check` 和 `build`，不得以新增 DOCX 测试替代 Task 1 至 Task 6 的回归。

Task 7 实施结果（见 [TASK-007 完成报告](./TASK_007_COMPLETION_REPORT.md)）：19 个测试文件、463 通过、3 条件跳过基线逐包可重复；WP0 修复了 React 组件测试遗留的 `act(...)` 一次性警告（归因见 TASK-007 WP0 报告），junction `EBUSY` 未复现；WP0 建立了 18 个可审计 DOCX 夹具并冻结兼容性矩阵、模型预算与依赖（Mammoth 1.12.1、JSZip 3.10.1、docx 9.7.1、Tiptap 3.29.2）；外部 Office 验证程序为 Microsoft Word 16.0.20228.20158。最终全量测试为 31 个测试文件、722 通过、5 条件跳过（均为真实符号链接权限条件）。

### 2.7 Task 8 实施前基线门禁与完成后基线

Task 8 将把工作区搜索从 TXT 扩展为 TXT + DOCX 混合搜索与富文本结果定位，并新增 DOCX 规范正文投影、候选分类与双层并发。开始实现前必须确认：

- Task 7 完成后的 31 个测试文件、722 通过、5 条件跳过基线可重复，或对当前环境差异形成可核对说明；
- 完整 `check` 与 `build` 依次执行并通过；
- TXT 搜索、结果定位、查找替换、DOCX 读取/编辑/保存、多标签、窗口关闭均无回归；
- DOCX 搜索正文的唯一语义来源为 `DocxDocumentModel` 规范正文投影，不把 DOCX 当作 UTF-8 TXT，也不直接搜索 OOXML、Mammoth HTML 或编辑器 DOM；
- 已按 [TASK-008 规划](./TASK_008_DOCX_WORKSPACE_SEARCH.md) 第 3.3 节完成投影与 ProseMirror 公开 API 技术验证（WP0 报告见 `TASK_008_WP0_REPORT.md`）；
- 每个 Task 8 工作包继续运行完整 `check` 和 `build`，不得以新增 DOCX 搜索测试替代 Task 1 至 Task 7 的回归。

Task 8 实施结果（见 [TASK-008 完成报告](./TASK_008_COMPLETION_REPORT.md)）：全部 8 个工作包逐包验收，最终全量测试为 38 个测试文件、833 通过、6 条件跳过（均为真实符号链接权限条件，拒绝分支由 mock 确定性覆盖）；`check` 与 `build` 依次执行退出码均为 0；接近上限工作区（800 TXT + 200 DOCX）实测搜索约 1.5–1.7 s、取消响应约 8 ms；开发模式与生产构建桌面冒烟均通过；第 3.6 节手工界面清单由项目所有者执行并通过。

### 2.8 Task 9 实施前基线门禁与完成后基线

Task 9 将引入工作区内文件管理写能力（新建、另存为、重命名、移动、删除、显示）与搜索失效。开始实现前必须确认：

- Task 8 完成后的 38 个测试文件、835 通过、6 条件跳过基线可重复，或对当前环境差异形成可核对说明；
- 完整 `check` 与 `build` 依次执行并通过；
- 工作区打开/刷新、TXT/DOCX 读取编辑保存、多标签会话、未保存保护、查找替换、混合搜索与结果定位均无回归；
- 所有写入与路径变化绑定主进程持有的当前工作区，经固定窄 IPC；renderer 不能提交根/绝对路径/危险开关；
- 已按 [TASK-009 规划](./TASK_009_BASIC_FILE_MANAGEMENT.md) 第 3.3 节完成 Windows 名称/冲突/大小写、trashItem、showItemInFolder、空白 DOCX 导出与另存为复用点、stable tabId 与 mutationEpoch 技术验证（WP0 报告见 `TASK_009_WP0_REPORT.md`）；
- 每个 Task 9 工作包继续运行完整 `check` 和 `build`，不得以新增文件管理测试替代 Task 1 至 Task 8 的回归。

Task 9 实施结果（见 [TASK-009 完成报告](./TASK_009_COMPLETION_REPORT.md)）：WP0-WP8 逐包验收；WP7 修复"重命名/新建不自动保留/补全扩展名"；最终全量测试为 **54 个测试文件、1016 通过、10 条件跳过**（均为真实符号链接/junction 权限条件，拒绝分支由 mock 适配器确定性覆盖）；`check` 与 `build` 依次执行退出码均为 0；开发模式与生产构建桌面冒烟通过；回收站恢复、外部占用与 case-only 均有实际证据（WP0/完成报告记录）；第 3.7 节手工界面清单由项目所有者执行并通过（用户确认"手动功能测试已通过"）。

### 2.9 Task 10 实施前基线门禁与完成后基线

Task 10 将把"当前文件查找替换"扩展为 TXT 与 DOCX 共用入口，并在 DOCX 编辑器会话中实现项目自有 ProseMirror 查找插件、装饰、替换事务与生命周期语义。开始实现前必须确认：

- Task 9 完成后的 54 个测试文件、1016 通过、10 条件跳过基线可重复，或对当前环境差异形成可核对说明；
- 完整 `check` 与 `build` 依次执行并通过；
- TXT 当前查找、工作区搜索与定位、DOCX 读取/编辑/保存、多标签、路径迁移、未保存保护均无回归；
- 已按 [TASK-010 规划](./TASK_010_DOCX_FIND_REPLACE.md) 第 3.3 节完成实时 textblock 投影、跨 marks 范围映射、Decoration、替换事务、模型预验证、read-only/degraded/saving 与性能策略的最小 Tiptap/ProseMirror 验证（WP0 报告见 `TASK_010_WP0_REPORT.md`）；
- 每个 Task 10 工作包继续运行完整 `check` 和 `build`，不得以新增 DOCX 当前查找测试替代 Task 1 至 Task 9 的回归。

Task 10 实施结果（见 [TASK-010 完成报告](./TASK_010_COMPLETION_REPORT.md)）：WP0-WP7 逐包验收；最终全量测试为 **62 个测试文件、1133 通过、10 条件跳过**（均为真实符号链接/junction 权限条件，拒绝分支由 mock 确定性覆盖）；`check` 与 `build` 依次执行退出码均为 0；开发模式与生产构建 Windows 冒烟通过（本包另记录 WP6 性能与泄漏观察）；任务第八节手工界面清单由项目所有者执行并通过（用户确认手动测试通过）。

### 2.10 Task 11 基线与完成后门禁

Task 11 修改前实际基线：62 个测试文件、1133 通过、10 条件跳过；`check` 与 `build` 退出码 0。技术与状态冻结见 `TASK_011_WP0_REPORT.md`。

Task 11 最终自动门禁除完整 Task 1–10 回归外，新增覆盖：

- 菜单方向键/Tab/Enter/Escape/外部点击/焦点恢复与对话框焦点圈定；
- 根、文件、文件夹上下文命令集合、危险操作顺序、saving 禁用与稳定路径参数；
- F2/Delete/F5/Ctrl+Shift+S、活动栏恢复侧栏及编辑器快捷键回归；
- 文件/目录到目录/根拖拽、no-op、自身/后代、同名、saving、非法目标、pending 防重、过期 epoch 与主进程失败；
- stable tabId、编辑器/查找会话、mutationEpoch 与既有 relocate/备份回归；
- 侧栏 180–420 px clamp、指针/键盘调整、折叠；DOCX 画布上限与工具栏宽/窄溢出；
- SVG/菜单/toast/对话框无障碍名称、forced-colors 与 reduced-motion 入口；
- 实际 Electron 截图脚本 `scripts/capture-ui-baselines.mjs`，产物位于 `docs/visual-baselines/task-011/`。
- 文件管理成功后的后台对账刷新不插入临时状态行、不重建文件树；手动刷新继续显示进度，
  手动与后台刷新单飞去重，避免快速状态切换造成界面闪烁；切换工作区后丢弃旧根目录的
  迟到刷新结果。

2026-08-22 最终结果：**69 个测试文件、1170 通过、10 条件跳过**；typecheck、lint、format:check、test、check、build 全部退出码 0；开发/生产 Electron 各 4 个进程存活 ≥10 秒且无启动错误，清理后残留 0。固定截图与缩放代理实际生成并检查。

2026-08-27 后续修复结果：窗口级 `Ctrl+F/H` 与文件管理静默后台刷新均已加入行为回归；
最终全量为 **69 个测试文件、1175 通过、10 条件跳过**，`check` 与 `build` 退出码 0。
项目所有者同日完成第 3.9 节的最终人工验收，确认功能交互、Windows 物理
100%/125%/150% 显示缩放、200% 文本缩放、高对比度、减少动画、屏幕阅读器及真实
文件操作未发现问题。

最终数量、命令退出码与人工终验证据见 Task 11 完成报告；不得用截图或 CSS 静态断言替代关键交互行为测试。

### 2.11 Task 12 WP8 验收结果

2026-09-09 从干净基准和锁文件安装依赖后，Electron `43.6.0` 的最终普通测试为
**73 个测试文件、1184 通过、10 条件跳过、0 失败**；typecheck、lint、format:check、test、
check、build、package:dir、package:win、package:verify 与 Electron E2E 均实际通过。E2E 为
1 个文件、4 个用例：启动/About、TXT 精确字节保存、DOCX 有效产物与备份、dirty 关闭取消。

内部 Windows 包还必须执行：

```powershell
.\scripts\npm.cmd run package:dir
.\scripts\npm.cmd run package:win
.\scripts\npm.cmd run package:verify -- --mode=win
.\scripts\npm.cmd run test:e2e
.\scripts\npm.cmd run release:manifest:unsigned
.\scripts\npm.cmd run release:verify:unsigned
```

`package:verify` 会读取完整 unpacked 树和 ASAR，拒绝源码、测试、日志、环境文件、凭据、
userData、`app-update.yml` 与不在 ASAR 索引中的 unpacked 文件，并验证 x64、身份、运行依赖和产物
命名。哈希必须在两个 EXE 均为 `NotSigned` 后生成并再次复验。

WP8 当前范围的 33/33 项均已关闭。验收主机内核 build 26200 属于 Windows 11，不能冒充 Windows 10
实测；项目所有者于 2026-09-10 将 Windows 10 x64 实机验证移到后续开发阶段，当前只要求理论兼容性
审计。最终 43.6.0 portable/NSIS 的 PE、架构、Electron、NSIS、依赖和源码平台门禁检查支持 Windows
10 x64，且安装/覆盖安装/卸载、路径、外部工作区不变性、残留、安全产品行为和既有 Word/WPS 文件
生命周期验收均无已知问题。私有仓库使用 GitHub Free，artifact attestation 不受支持且真实原因已
记录；精确产品提交 `98b05b4` 的 main push CI 已通过。理论审计不得写成 Windows 10 实机验证；逐项
映射见
[TASK-012 完成报告](./TASK_012_COMPLETION_REPORT.md)。

## 3. 开发模式界面验收

启动应用：

```powershell
.\scripts\dev.cmd
```

手动确认：

- Electron 窗口能够出现且不是空白页；
- 标题和中央欢迎区域显示“文枢”；
- 顶部菜单占位、左侧活动栏、工作区侧栏、中央欢迎区域和底部状态栏均可见；
- 状态栏显示 `Windows · Electron <版本>`；
- 尚未打开工作区时显示“尚未打开文件夹”和“打开文件夹”入口；
- 点击“打开文件夹”会打开原生目录选择器；
- 取消选择不会报错或清除已经打开的工作区；
- 成功选择后显示工作区名称、路径和文件树；
- 多层目录可以展开和折叠，空目录具有明确状态；
- 点击“刷新”后可以看到外部增加或删除的文件；
- 再次选择文件夹可以切换工作区；
- 普通文件、符号链接和其他叶节点只显示名称与类型，不读取正文；
- 调整窗口大小后，中央区域仍然可见；
- 启动终端没有 preload、React 或资源加载错误；
- 关闭窗口后应用正常退出。

### 3.1 TXT 读取与编辑验收

准备一个包含多层目录的测试文件夹，并在根目录与子目录中各放置一个 UTF-8 TXT（建议包含中文、多行和空白），另准备一个空 TXT、一个非 TXT 文件和一个超过 5 MiB 的 TXT。

- 点击根目录 TXT：中央区域先显示"正在读取 <文件名>…"，随后显示以文件名命名的单个活动标签和可编辑正文；
- 正文可用 CodeMirror 编辑：输入、删除、复制粘贴、撤销和重做均生效；
- 展开子目录并打开嵌套 TXT，内容正确且标签标题为文件名；
- 打开空 TXT：标签正常显示，正文区域为空且可输入；
- 连续快速选择两个 TXT：生成两个标签，最后选择的为活动标签（多标签语义见 3.3）；
- 点击非 TXT 文件、目录或符号链接：不读取正文，中央区域内容不变；
- 删除一个尚未打开的 TXT 后点击它：显示错误提示，应用不崩溃；
- 打开超过 5 MiB 或非 UTF-8 编码的 TXT：显示明确错误提示，不显示部分正文；
- 已有正文时打开失败文件：原正文保留并显示非阻塞错误横幅；
- 点击"刷新"：当前编辑正文保持可见；
- 成功切换工作区：旧正文清除并回到欢迎状态；
- 取消切换工作区：旧正文保留；
- 开发者控制台没有未处理异常；
- 测试过程中确认应用没有创建、修改或删除任何测试文件（保存用例除外）。

### 3.2 编辑与保存验收

- 首次修改后标签出现未保存标记（●），工具条显示"未保存"；
- 点击"保存"与按 `Ctrl+S` 发起同一保存流程；未修改时保存不触发写入；
- 保存期间显示"正在保存…"，快速重复保存不会并发写入；
- 保存成功且期间无新编辑：未保存标记消失，显示"已保存"；
- 保存期间继续编辑：旧保存成功后未保存标记仍存在，再次保存基于新版本；
- 保存失败（只读文件、权限错误等）：正文与未保存标记保留，显示"保存失败"；
- 用其他编辑器（如记事本）修改已打开文件后再保存：显示"外部冲突"，外部内容未被覆盖，本地正文保留；点击"重新读取"并确认后载入磁盘版本；
- BOM 文件保存后 BOM 保留；LF 与 CRLF 文件保存后换行风格不变；
- 混合换行文件保存：先弹出"确认换行规范化"对话框，确认后才按主要换行风格统一保存；
- 有未保存修改时关闭标签 / 切换工作区 / 关闭窗口：分别验证"放弃修改"与"取消"分支，取消后正文、文件、工作区和选择状态全部保留；
- 保存后继续输入，确认未保存标记重新出现；
- 刷新工作区后当前编辑内容保持；
- 工作区目录中没有遗留 `.wenshu-*` 临时文件。

### 3.3 多标签页与独立会话验收

- 从文件树连续打开三个不同目录的 TXT：标签顺序与点击顺序一致，最后打开的标签为活动标签；
- 重复点击已打开文件只激活原标签，不重复读取、不重置正文；
- 打开两个同名不同路径的文件：两个标签并存，`title` 提示可区分完整路径；
- 在不同标签中输入不同内容并来回切换：正文与未保存标记各自独立、互不污染；
- 每个标签的光标、选区、滚动位置和撤销历史独立；普通切换不清空历史、不产生未保存标记；
- 保存一个标签不会清除其他标签的未保存标记；保存 A 时切到 B 继续编辑，两个标签最终状态正确；
- 用其他编辑器修改 A 后保存：A 显示外部冲突，B 仍可正常编辑和保存；
- 关闭 clean 标签无需确认；关闭 dirty 标签需"放弃修改/取消"确认；关闭按钮与 `Ctrl+W` 行为一致；
- 关闭活动标签后优先激活右侧标签，没有右侧时激活左侧；关闭最后一个标签回到欢迎页；
- 有多个 dirty 标签时切换工作区与关闭窗口：确认文案包含未保存标签数量，分别验证放弃与取消分支；
- 正在保存的标签无法被关闭、随工作区切换或窗口关闭丢弃，界面提示等待保存完成；
- 刷新文件树不影响已打开标签；
- 连续打开接近 20 个普通 TXT：标签栏可横向滚动，切换无明显等待；
- 开发者控制台与启动终端没有未处理异常、正文、绝对路径或临时文件名日志。

Task 6 已完成并交付工作区 TXT 搜索与当前文件查找替换。当前仍不应出现自动保存、DOCX 编辑、文件管理或会话恢复功能。

### 3.4 搜索与查找替换验收

准备一个包含多层目录的测试文件夹，在根目录与子目录中放置多个 UTF-8 TXT（含中文、英文、emoji、LF、CRLF、空行与长行），另准备同名不同路径文件、空 TXT、非 TXT、非 UTF-8、超过 5 MiB、符号链接和不可读目录/文件，以及接近 1000 个小 TXT 的性能工作区。

- 活动标签内按 `Ctrl+F` 打开查找面板、`Ctrl+H` 打开面板并聚焦替换输入；普通文字查询、大小写选项、上一个/下一个、替换当前项与全部替换均生效；
- 查找不修改正文、不制造未保存标记；替换产生未保存标记、可撤销/重做并可继续显式保存；
- 多标签切换保持各自查找条件、当前匹配、选区和撤销历史，互不污染；关闭标签或切换工作区后查找会话随标签清理；
- 既有 `Ctrl+S`、`Ctrl+W`、撤销/重做和会话恢复行为无回归；
- 点击活动栏"搜索"或按 `Ctrl+Shift+F` 打开搜索侧栏；无工作区时显示空状态且不发起搜索；
- 搜索普通中文、英文与 emoji，结果行列与片段正确；搜索无结果、取消、快速连续提交和到达结果上限时界面状态明确；
- 单文件失败不阻塞其他结果，界面展示跳过统计；
- 点击未打开、已打开、同名不同路径和 loading 状态的文件结果：打开或激活唯一标签，读取完成后在结果有效时选中匹配、滚动到可视区域并聚焦编辑器；
- 搜索后外部修改文件或编辑已打开标签使范围变化，再点击旧结果：只显示"搜索结果已过期"提示，不错误定位、不修改正文；
- 搜索进行中切换工作区：旧结果不出现；接近 1000 文件时界面仍可操作，取消能够在合理时间内生效；
- 控制台和终端无未处理异常、查询正文、命中正文、绝对路径或文件句柄日志；工作区没有新增缓存、索引或临时文件；
- 开发模式与生产构建均完成相同关键路径冒烟。

### 3.5 DOCX 验收清单（Task 7 已并入当前能力）

> Task 7 已完成（见 [TASK-007 完成报告](./TASK_007_COMPLETION_REPORT.md)）；本节为 DOCX 能力的手工界面验收清单，自动可覆盖部分已由 `tests/docx/` 与 `tests/document/docx-*.test.tsx` 固化。

准备 [TASK-007 规划](./TASK_007_DOCX_BASIC_EDIT_SAFE_SAVE.md) 第 3.3 节定义的普通、复杂、损坏、超限和 round-trip DOCX 夹具，并准备至少一种外部 Office 程序（本机为 Microsoft Word 16.0.20228.20158）。

- 文件树可以选择普通 `.docx`；目录、符号链接、`.docm`、伪装扩展名和其他类型不触发 DOCX 读取；
- 普通 DOCX 先显示加载状态，再以唯一标签显示段落、标题、粗体、斜体、下划线、基础字号、颜色、列表和对齐；
- 中文、英文、emoji、空段落和多段落显示正确；普通文档在数秒内打开且界面保持可操作；
- 由 Windows/WPS 新建但尚未物化的 0 字节 `.docx` 作为空白文档打开，可输入并首次保存；保存后目标为可重新打开的有效 DOCX，滚动备份保留保存前的 0 字节占位内容；
- WPS 保存且包含 `<w:documentProtection w:enforcement="0"/>` 的普通 DOCX 保持可编辑；`enforcement="1"`、`true` 或 `on` 的保护文档进入只读；
- 损坏、加密、缺失关键 OOXML 部件、超过 20 MiB 或超出解压/模型预算的文件显示稳定错误，不显示部分可编辑内容；
- 含图片、表格、页眉页脚、批注、修订、公式或嵌入对象的文档显示确定兼容性等级和警告；`degraded` 在编辑或首次保存前确认，`read-only` 不可保存；
- 基础格式工具栏、复制、剪切、粘贴、撤销和重做均生效；无实际修改不制造 dirty；
- TXT 与 DOCX 可以混合打开；每个标签的正文、选择、滚动、历史、dirty、保存和错误状态独立；
- 非活动 DOCX 标签不参与布局；切换 TXT/DOCX 或多个 DOCX 时只显示当前标签正文；
- 外部重读比较完整结构化模型：即使正文文字相同，粗体、斜体、颜色、字号或对齐变化也会更新编辑器，且重读本身不产生 dirty；
- 点击保存或按 `Ctrl+S` 发起同一 DOCX 保存流程；无 dirty 不写盘，同一标签不并发保存；
- 保存前在同目录生成 `<文件名>.wenshu.bak`，其内容等于本次保存前原文件；备份失败时目标不变化；备份持续保留但不在工作区文件树显示；
- 保存期间显示明确状态；保存期间继续编辑时，旧保存成功不清除后续 dirty；
- 用外部 Office 修改已打开文件后保存，文枢报告 revision 冲突，不覆盖外部文件也不丢本地编辑；
- 注入导出、产物验证、临时写入、sync、close 或替换失败，原 DOCX 仍能由外部 Office 打开，编辑器内容和 dirty 保留；
- 保存成功后文枢可以重新打开产物，外部 Office 可以打开，受支持正文和格式符合兼容矩阵；
- dirty DOCX 关闭、切换工作区和关闭窗口继续执行未保存确认；正在保存的 DOCX 不会被丢弃；
- 工作区没有遗留本次 `.wenshu-*` 临时文件；滚动备份除外；
- 不发生远程关系请求，不执行宏、脚本或嵌入内容；
- 控制台和终端无未处理异常、正文、OOXML、绝对路径、Buffer、句柄或临时名称日志；
- 开发模式与生产构建完成相同关键路径，并记录外部 Office 名称、版本和观察结果。

2026-08-11：项目所有者使用 WPS Office 完成最新版手工验收，上述双向打开保存、冲突、
备份恢复、中文斜体和标签隔离路径均通过；WPS 具体版本未记录。

### 3.6 工作区 DOCX 搜索与富文本定位验收（Task 8 已并入当前能力）

> Task 8 已完成（见 [TASK-008 完成报告](./TASK_008_COMPLETION_REPORT.md)）；本节为 DOCX 搜索与富文本定位的手工界面验收清单，自动可覆盖部分已由 `tests/search/`（混合搜索、定位、生命周期）与 `tests/document/`（宿主二次校验）固化。

准备一个混合工作区：根目录与子目录放置多个 UTF-8 TXT 与普通 DOCX（普通、WPS 生成、read-only、degraded、损坏、0 字节占位、同名不同路径），并准备接近 1000 总文件（其中接近 200 DOCX）的性能工作区。

- 活动栏"搜索"/`Ctrl+Shift+F` 打开搜索侧栏；无工作区时显示空状态且不发起搜索；
- 一次查询同时搜索 TXT 与 DOCX：结果按文件分组显示相对路径、TXT/DOCX 类型标识、1-based 行列与安全片段；DOCX 行列明确为提取正文坐标并提示不是 Word 页面坐标；
- 中文、英文、emoji 与大小写开关均生效；无结果、取消、连续提交与截断（含 DOCX 候选上限）状态明确；
- 统计（扫描/命中/匹配/跳过）准确；单文件失败（损坏/超限/加密/伪装）隔离为跳过，不阻塞其他结果；
- 点击未打开 / 已打开 / loading / dirty / read-only / degraded 的 DOCX 结果：打开或激活唯一标签并定位；read-only 可定位不可编辑；degraded 定位不自动确认兼容性；dirty 但原范围未变可定位且 dirty 保留；
- 段落、标题、格式跨 run、项目符号、编号与嵌套列表、emoji 的定位选区准确；
- 仅改格式（粗体/斜体/字号/颜色/对齐）后投影不变仍可定位；匹配前插入/删除正文、结构变化、范围越界或文本不符时只显示"搜索结果已过期"，不错误定位、不修改正文；
- 外部 Office 修改文件后点击旧结果：只提示过期（Word/WPS 外部修改 → revision 变化路径）；
- 搜索中切换工作区与关闭窗口：旧结果不出现、任务清理、标签与未保存保护不受影响；
- 接近 1000 总文件（含接近 200 DOCX）时界面保持可操作，取消在合理时间内生效；
- 控制台和终端无未处理异常、查询/命中正文、OOXML、绝对路径或文件句柄日志；工作区没有新增备份、索引、缓存或临时文件。

### 3.7 文件管理验收清单（Task 9 已并入当前能力）

> Task 9 已完成（见 [TASK-009 完成报告](./TASK_009_COMPLETION_REPORT.md)）；本节为文件管理的手工界面验收清单，自动可覆盖部分已由 `tests/workspace/`（文件管理 UI、relocate/trash controller、mutationEpoch 生命周期）、`tests/file-management/`（契约与解析）与 `tests/document/`（另存为、路径迁移）固化。

准备一个包含多层目录的测试工作区，根目录与子目录放置 TXT、DOCX 与普通文件；另准备 Word/WPS（或等价外部占用程序）。

- 在工作区根与选中文件夹内新建 TXT、基础 DOCX 与文件夹；新建 TXT/DOCX 自动打开为唯一干净标签并可编辑保存；新建文件夹保持选中且可展开；
- 新建 TXT/DOCX 输入无扩展名名称自动补全 `.txt`/`.docx`（如 `会议纪要` → `会议纪要.txt`）；输入其他扩展名被拒绝并保留输入；
- 新建与已存在条目同名：明确"目标已存在"错误，不覆盖、不自动改名；
- 另存为：选择工作区内目标文件夹并输入名称；目标不存在时安全创建；目标存在时先显示覆盖确认，确认后覆盖；覆盖确认期间外部修改目标会要求重新确认；
- 另存为成功后源文件保持不变，当前标签迁移到新路径并保持光标/选区/撤销历史；保存期间继续编辑的标签保持 dirty；
- 重命名文件与文件夹（含 Windows 只改大小写重命名）；重命名 TXT/DOCX 自动保留原扩展名，改为其他扩展名被拒绝；已打开文档的标签顺序、活动状态、dirty、正文/模型与编辑器会话保留；
- 移动文件与文件夹到工作区内其他目录；目录不能移动到自身或后代；移动已打开目录后其下全部打开标签路径同步迁移；
- 删除文件与非空目录：先显示包含路径、类型与受影响未保存标签数的确认；删除成功进入 Windows 回收站，可从回收站恢复；dirty 删除需明确放弃，saving 删除被阻止并提示等待；
- 删除失败（权限/占用）不提前关闭标签、不降级永久删除；
- 对文件、目录与工作区根执行"在资源管理器中显示"，且不开放任意 shell；
- 任一成功新建/另存为/重命名/移动/删除后：文件树自动刷新；若此前有 completed 工作区搜索，搜索结果立即清空；搜索中执行文件操作会取消在途搜索；失败、取消与"在资源管理器中显示"不清空有效结果；
- 快速重复点击同一操作按钮不会产生并发写（提交期间按钮禁用）；
- 用外部程序独占打开一个文件（如 Word/WPS 或记事本占用）后执行删除/重命名：显示稳定错误，不崩溃、不残留半成品；
- 操作取消后焦点回到原触发控件，错误保留输入可修改重试；partial failure（如主文件与伴随备份未同时删除）显示"操作部分完成"并强制刷新；
- 工作区没有遗留 `.wenshu-*` 临时文件；DOCX 备份位置符合规则（`<目标>.wenshu.bak` 或伴随原文件）；
- 控制台和终端无未处理异常、正文、绝对路径、临时名或原始异常日志；
- 开发模式与生产构建完成相同关键路径冒烟。

### 3.8 当前 DOCX 查找替换验收清单（Task 10 已并入当前能力）

> Task 10 已完成（见 [TASK-010 完成报告](./TASK_010_COMPLETION_REPORT.md)）；本节为当前 DOCX 查找替换的手工界面验收清单，自动可覆盖部分已由 `tests/search/`（面板、App 集成、生命周期）、`tests/document/`（plugin、替换、路径迁移）与 `tests/docx/`（纯模块、WP6 性能/泄漏观察）固化。

在活动 DOCX 标签中（普通/read-only/degraded/dirty/saving/save-error/conflict 状态）：

- 按 `Ctrl+F` 打开"查找与替换"侧栏并聚焦查找输入，按 `Ctrl+H` 打开并聚焦替换输入；TXT 与 DOCX 共用同一入口，同一时刻只显示活动 kind 的面板；
- 输入普通单行查询后即时搜索实时未保存正文：段落、标题、跨 marks run、项目符号/编号/嵌套列表、中文与 emoji 均可匹配；计数显示"第 n / m 处"，超过 2000 处显示截断提示；
- 普通匹配与当前匹配高亮可辨识（当前匹配不只依赖颜色）；`F3`/`Shift+F3`、Enter/Shift+Enter 与按钮循环导航，导航设置选区、滚动并聚焦编辑器；Escape 关闭面板并恢复焦点；
- 查找、导航、开关面板不修改正文、不制造 dirty、不进撤销历史；read-only 可查找不可替换并显示原因；
- degraded 未确认可查找不可替换并显示原因；确认"继续编辑并保存"（绑定当前 revision）后同一编辑器即可替换；重新读取不同 revision 后旧确认失效、替换重新禁用；
- 可编辑 DOCX 替换当前项与全部替换：执行瞬间重新复验实时范围，非空替换继承匹配起点字符格式（跨不同 marks 只取起点），标题/列表/对齐与周围格式保持；全部替换为单次可撤销/重做事务；0 匹配无操作，2001+ 整体拒绝且无部分替换；模型预算失败 0 修改；
- 替换成功进入 dirty 并可经既有保存流程显式保存（revision 冲突、滚动备份、安全替换不变）；saving 期间替换在旧保存完成后仍保持 dirty；
- 多个 TXT/DOCX 标签的查询、选项、当前匹配、装饰与历史互不污染；关闭标签后重开是新会话；工作区切换清理全部会话与面板；
- 重命名、移动、另存为后查找会话与装饰保持（不重建编辑器）；工作区结果定位与 mutationEpoch 变化不污染当前查找状态；
- 控制台与终端无未处理异常、查询/替换文本、正文或绝对路径日志；工作区无新增临时残留；
- 开发模式与生产构建完成相同关键路径冒烟。

### 3.9 Task 11 桌面外壳、缩放与交互验收

固定截图尺寸：`1280×820`、`900×600`。正常模式可执行：

```powershell
.\scripts\npm.cmd run build
.\scripts\npm.cmd exec -- electron .\scripts\capture-ui-baselines.mjs
```

人工核对：

- 顶部只有一套中文菜单；“文件/编辑/视图/帮助”均为真实命令，“关于”显示平台/Electron，状态栏不再显示版本；
- 文件/搜索活动栏为 SVG 且有 tooltip/无障碍名称，无伪设置入口；侧栏可拖至 180/420 px、键盘 Home/End 调整、折叠与恢复；
- 空工作区、长文件树、20 层目录、超长文件名、多标签在 1280×820 与 900×600 下无编辑区横向溢出或不可达操作；
- 根/空白、普通文件、文件夹右键菜单内容与启用状态正确；Shift+F10/菜单键、方向键、Enter、Escape、外部点击和焦点恢复可用；
- F2、Delete、F5、Ctrl+Shift+S 不绕过 saving、dirty、确认、冲突与权限门禁；Ctrl+F/H/S/W 与撤销重做无回归；
- 文件/目录拖到目录与根成功；当前位置 no-op；自身/后代、同名、saving、非法类型拒绝；Escape/dragend/窗口失焦/工作区切换清理；失败显示持久错误；
- 拖拽/重命名/移动/删除/另存为后 stable tabId、dirty、选区、滚动、撤销、当前查找、DOCX 备份和工作区搜索失效语义正确；
- DOCX 是连续居中写作列，不显示分页承诺；窄工具栏出现“更多格式”；TXT 保持全高；
- 搜索结果呈“文件→匹配”，片段优先、行列次要；普通匹配、当前匹配、输入焦点和按钮焦点可区分；
- 成功 toast 限时且可关闭；错误/冲突/partial failure 不自动消失；菜单、图标按钮、拖拽目标、toast 和对话框有明确名称；
- 在 Windows 100%/125%/150% 显示缩放、200% 文本缩放、高对比度和减少动画下重复上述核心路径。

物理显示缩放会改变 Windows 会话级设置，自动化环境不应擅自修改。无法在当前会话切换时，必须在完成报告中保留“待项目所有者确认”，不得写成已通过。

Task 11 的上述人工清单已于 2026-08-27 由项目所有者执行并确认通过；最终证据见
[TASK-011 完成报告](./TASK_011_COMPLETION_REPORT.md)。

## 4. 生产构建验收

生成生产构建：

```powershell
.\scripts\npm.cmd run build
```

验收标准：命令退出码为 `0`，并生成以下关键产物：

```text
out/
├─ main/index.js
├─ preload/index.js
└─ renderer/
   ├─ index.html
   └─ assets/
```

运行生产构建：

```powershell
.\scripts\npm.cmd exec -- electron .
```

生产窗口应显示与开发模式一致的单一中文菜单、活动栏、可调侧栏、编辑区和低权重状态栏。除 Task 7–10 的 DOCX/搜索/文件管理/当前查找路径外，按 3.9 验证上下文菜单、键盘命令、内部拖拽、DOCX 工具栏溢出、toast 和折叠恢复。关闭窗口即可结束进程。

## 5. 安全边界检查

当前阶段应持续满足：

- `BrowserWindow` 使用 `nodeIntegration: false`；
- `contextIsolation: true`；
- sandbox 保持启用；
- preload 只暴露只读运行环境数据、固定的 `workspace.open()` / `workspace.refresh()`、`document.readText(relativePath)` / `document.saveText(request)` 以及 `window` 关闭协调窄协议（`setDirtyState` / `requestClose` / `cancelClose` / `onCloseRequested`）；
- 渲染进程不直接导入 Node.js 文件系统 API；
- 不向渲染进程暴露 `ipcRenderer` 或通用 IPC 调用器；
- 工作区 IPC 不接受渲染进程提供的绝对路径；
- `document.readText` 与 `document.saveText` 只接受文件树快照中的规范相对路径，主进程对其重新执行格式、边界、符号链接、类型、大小与 UTF-8 校验；
- 文档读取只使用只读文件系统 API（`lstat`、`realpath`、有界 `read`）；
- TXT 保存执行：工作区与路径重校验、SHA-256 内容版本冲突检测（不一致返回 `CONFLICT` 且不写盘）、同目录排他临时文件、完整写入、`sync` 刷盘、关闭与同文件系统 `rename` 替换；失败时原文件不删除不截断并尽力清理临时文件；
- 保存 IPC 入口拒绝多余危险字段（工作区根、绝对目标、临时路径、编码、替换策略等）并对参数执行运行时校验；
- 窗口关闭协调不获得文件内容、工作区根路径或通用 IPC 能力；
- 跨进程结果不包含原始异常、Buffer、文件句柄或调用栈。

Task 7 已实施并持续验证：

- preload 只新增固定 `document.readDocx(relativePath)` / `document.saveDocx(request)`，不暴露 ZIP、OOXML、任意 HTML 或通用文件系统能力；
- DOCX 请求不接受工作区根、绝对路径、临时/备份路径、强制覆盖或跳过备份策略（IPC 字段白名单 + 模型运行时校验）；
- 主进程将 DOCX 作为不可信 ZIP 处理，对压缩大小、条目、解压大小、关键 XML 和中间模型执行固定预算；
- 不访问外部关系，不执行宏、脚本、字段或嵌入对象；
- DOCX 保存执行原始字节 revision 冲突检测、同目录滚动备份、生成产物重新验证、排他临时写入、`sync`、关闭与安全替换；
- 备份失败、产物验证失败或替换前 revision 再次变化时不得修改目标；
- 跨进程结果不包含原始 OOXML、未清洗 HTML、库实例、Tiptap 实例、Buffer、文件句柄或调用栈。

Task 8 已实施并持续验证：

- preload 只复用固定 `search.textWorkspace(request)` / `search.cancelTextWorkspace(request)`，不新增通用 IPC；请求不接受工作区根、绝对路径、glob、扩展名、编码、并发或资源预算字段；
- 工作区搜索根路径只来自主进程 `workspace-session`；目录遍历不跟随符号链接/junction，候选读取每次重新执行受控校验（TXT 复用 `readTextDocument`，DOCX 复用 `readDocxDocument`）；
- DOCX 搜索正文只来自合法 `DocxDocumentModel` 的规范正文投影（`src/shared/docx-search-text.ts`），不把 DOCX 当作 UTF-8 TXT，不直接搜索 OOXML、Mammoth HTML 或编辑器 DOM；
- 资源上限固定：总候选 1000（其中 DOCX 200）、单文件 200、总匹配 2000、查询 256、总并发 4（其中 DOCX 并发 2），截断原因优先级固定；
- 搜索只读：不调用任何写入、创建、重命名、删除 API，不创建备份、临时文件、索引或缓存；
- 结果定位必须通过 kind、revision、范围与匹配文本双重校验（App 实时投影校验 + DOCX 宿主同规则投影二次校验），使用 ProseMirror 公开命令设置选区、滚动与聚焦；任何过期、结构变化或映射失败只显示非破坏性提示；
- 查询、命中正文、完整投影、模型、原始异常与绝对路径不进入日志或跨进程错误。

Task 9 已实施并持续验证：

- preload 只新增固定 `createText/createDocx/createDirectory/relocate/trash/reveal/saveTextAs/saveDocxAs`（kind 由 preload 固定注入），不暴露 ipcRenderer、通用 invoke 或任意通道；请求精确形状校验，拒绝多余字段、根/绝对路径、临时/备份路径、shell 参数与 `force`/`overwrite`/`skipValidation` 等危险开关；
- 工作区根只来自主进程 `workspace-session`，处理器绑定发送窗口；同一窗口写操作经 mutation coordinator 串行，同一时刻最多一个在途写操作；
- 新建/另存为目标经源/目标逐段 lstat/realpath（拒绝任一 symlink/junction）与 realpath 边界检查；目标存在一律 `TARGET_EXISTS`（排他创建判定，不覆盖、不自动改名）；发布前复验后同文件系统 rename 发布；
- 写文件使用目标同目录排他临时文件、完整写入、`sync`、关闭与不覆盖发布，失败尽力清理；Windows case-only 重命名使用不可预测中间名两步 + 回滚；
- DOCX 另存为/重命名保持产物验证与备份语义：另存为覆盖前创建目标滚动备份，重命名/移动伴随 `<源>.wenshu.bak` 迁移，失败回滚或明确 `PARTIAL_FAILURE`；
- 删除统一调用可注入 `shell.trashItem`（生产为 Electron shell.trashItem），严禁 unlink/rm/rmdir 永久删除降级；删除根/link/other/内部恢复文件拒绝；
- 另存为两阶段覆盖确认：目标存在第一次只返回 `TARGET_EXISTS` 与受控目标 revision，确认后的第二次请求必须携带 `expectedTargetRevision`，发布前再次比较，变化返回 `CONFLICT` 重新确认；
- 成功 mutation（create/save-as/relocate/trash）递增 `mutationEpoch`：取消活动搜索、清空 completed/cancelled/error 结果与定位；失败、取消、reveal 与刷新失败不递增；迟到搜索结果必须同时校验 requestId + workspaceEpoch + mutationEpoch；
- 跨进程结果不包含正文、模型、绝对路径、临时名、原始异常或调用栈；日志不泄漏上述内容。

Task 10 已实施并持续验证：

- preload / IPC / DesktopApi 集合未扩大：仍为 17 个固定 `ipcMain.handle`（workspace 6 + document 4 + save-as 2 + search 2 + window 3），preload 契约测试通过；查询、替换文本、匹配范围与正文不跨 IPC、不进日志；
- DOCX 当前查找的正文唯一来源是活动 Tiptap/ProseMirror 实时文档，不读取磁盘、不搜索模型旧快照、不解析 OOXML/Mammoth HTML/编辑器 DOM；
- 查找/导航/关闭面板只 dispatch 非文档事务（selection + plugin meta），不修改正文、不 dirty、不进撤销历史；替换经公开 transaction API 构建，dispatch 前对候选文档执行 `tiptapJsonToDocxModel` 结构与序列化预算验证（失败 0 dispatch、0 dirty）；
- 替换权限由共享标签状态派生并在命令内防御性拒绝（read-only / degraded 未确认 / editor 不可编辑），不依赖按钮 disabled 作为唯一保护；
- 全部替换 ≤2000 项、逆序单事务、一次 undo/redo；2001+ 或模型超预算整体拒绝，无部分替换；
- 每标签查找状态绑定稳定 tabId，重命名/移动/另存为不重建编辑器；关闭标签、工作区切换与 editor 销毁清理插件订阅与 controls，无计时器（仅微任务调度）；
- 不记录查询、替换文本、正文、完整模型或绝对路径到日志；无新增第三方依赖。

这些约束主要由代码审查、类型隔离、单元测试和真实窗口启动共同验证。后续新增桌面能力时，应为每个具体用例设计独立接口和测试。

## 6. 推荐验收顺序

```powershell
.\scripts\bootstrap.cmd
.\scripts\npm.cmd ci
.\scripts\npm.cmd run check
.\scripts\npm.cmd run build
.\scripts\dev.cmd
```

不要并行运行 `check` 和 `build`：electron-vite 构建期间会创建临时配置文件，与 ESLint 扫描并发时可能产生无意义的文件竞争。
